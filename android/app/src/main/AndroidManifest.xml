<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/ic_btn_speak_now"
        android:label="@string/app_name"
        android:roundIcon="@android:drawable/ic_btn_speak_now"
        android:supportsRtl="true"
        android:theme="@style/Theme.KokoroTTS">

        <!-- System-wide Text to Speech Engine Service -->
        <service
            android:name=".KokoroTtsService"
            android:label="@string/tts_engine_name"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.TTS_SERVICE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <meta-data
                android:name="android.speech.tts"
                android:resource="@xml/tts_engine" />
        </service>

        <!-- Foreground Service for Background Audiobook Playback & Lock Screen Controls -->
        <service
            android:name=".reader.service.BookPlaybackService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- Voice Selection & Engine Settings Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/title_settings">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Dedicated Ebook Reader Activity -->
        <activity
            android:name=".reader.ui.ReaderActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:label="Kokoro Ebook Reader" />

    </application>

</manifest>
